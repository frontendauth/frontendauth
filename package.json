{
  "name": "foundation-libsass-template",
  "version": "0.1.1",
  "scripts": {
    "build": "grunt build",
    "deploy": "grunt deploy",
    "postinstall": "bower cache clean && bower install && bundle install",
    "start": "npm run build && open dist/index.html && npm run watch",
    "watch": "grunt"
  },
  "dependencies": {},
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-contrib-copy": "~0.7.0",
    "grunt-contrib-jade": "~0.14.0",
    "grunt-contrib-watch": "~0.5.3",
    "grunt-deploy": "~0.1.7",
    "grunt-sass": "^1.0.0",
    "grunt-ssh": "~0.12.0",
    "grunt-ssh-deploy": "~0.2.2",
    "node-sass": "^3.2.0"
  }
}
