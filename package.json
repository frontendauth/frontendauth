{
  "name": "foundation-libsass-template",
  "version": "0.1.1",
  "scripts": {
    "build": "grunt build",
    "deploy": "",
    "postinstall": "bower cache clean && bower install && budle install",
    "serve": "npm run build && open dist/index.html && npm run watch",
    "watch": "grunt"
  },
  "dependencies": {},
  "devDependencies": {
    "node-sass": "~1.2.3",
    "grunt": "~0.4.1",
    "grunt-contrib-watch": "~0.5.3",
    "grunt-sass": "~0.17.0",
    "grunt-contrib-copy": "~0.7.0"
  }
}
